import { ObserveVisibiliDirective } from './observe-visibili.directive';

describe('ObserveVisibiliDirective', () => {
  it('should create an instance', () => {
    const directive = new ObserveVisibiliDirective();
    expect(directive).toBeTruthy();
  });
});
